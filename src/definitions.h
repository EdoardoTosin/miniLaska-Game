#define DIM (7)
#define HEIGHT (3)
#define ROW (65)
#define MAX_DEPTH (4)

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#ifdef _WIN32
#define CLEAR "cls"
#else
#define CLEAR "clear"
#endif
